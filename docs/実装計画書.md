# QRコード受付システム 実装計画書

本ドキュメントは、要件定義書・アーキテクチャ設計書・API設計書・リポジトリ構造の定義書に基づき、実装タスクをフェーズごとに分解したチェックリスト形式の実装計画書です。初心者でも順番に進められるよう、環境準備から最終デプロイまでを網羅しています。

---

## 凡例

- `[ ]` … 未完了
- `[x]` … 完了
- **参考** … 関連ドキュメントの参照先
- **参照すべきドキュメント** … 各フェーズの冒頭に記載。実装前に該当ドキュメントの該当箇所を確認すること

---

## Phase 0: 環境準備

開発に必要なツールをインストールし、プロジェクトの土台を整えます。

> **参照すべきドキュメント**
> - [要件定義書](./要件定義書.md) … プロジェクト全体像、技術スタックの把握
> - [リポジトリ構造の定義書](./リポジトリ構造の定義書.md) … Clasp のインストール・設定、gh コマンドの前提

### 0.1 必須ツールのインストール

- [x] **Node.js** をインストールする（Clasp の実行に必要）
  - 参考: https://nodejs.org/
- [x] **Clasp** をグローバルインストールする
  ```bash
  npm install -g @google/clasp
  ```
- [x] **Git** をインストールする
  - 参考: https://git-scm.com/
- [x] **GitHub CLI (gh)** をインストールする
  ```bash
  # macOS (Homebrew)
  brew install gh
  ```
- [x] `gh auth login` で GitHub に認証する

### 0.2 プロジェクトの初期確認

- [x] プロジェクトフォルダに移動する
- [x] 既存の `.clasp.json` の `scriptId` を確認する（GAS プロジェクトと紐付いているか）
- [x] `docs/` フォルダ内の設計書を一読する

---

## Phase 1: スプレッドシート・GAS 基盤の構築

空のスプレッドシートを作成し、GAS プロジェクトを紐付けます。**シート・ヘッダーの作成は Phase 2 の GAS「スプレッドシート初期設定」で行うため、本フェーズでは手動でシートを作成しない。**

> **参照すべきドキュメント**
> - [要件定義書](./要件定義書.md) 第3章・第4.1節 … データ構造、スプレッドシート初期設定の機能
> - [API設計書](./API設計書.md) 第7章 … スプレッドシート列とAPIフィールドのマッピング
> - [リポジトリ構造の定義書](./リポジトリ構造の定義書.md) 第4章 … .clasp.json、rootDir の設定

### 1.1 スプレッドシートの作成

- [x] 新規 Google スプレッドシートを作成する（空のままでよい）
- [x] スプレッドシート名を「オリエンテーション受付システム」等に設定する

### 1.2 GAS プロジェクトの紐付け

- [x] スプレッドシートから「拡張機能」→「Apps Script」を開く
- [x] GAS プロジェクトが自動作成されることを確認する
- [x] 「プロジェクトの設定」から **スクリプト ID** をコピーする
- [x] ローカルの `.clasp.json` に `scriptId` を設定する（既存の場合は確認のみ）
- [x] `.clasp.json` に `"rootDir": "gas"` を設定する（gas フォルダを rootDir にする場合）

---

## Phase 2: GAS 実装

Google Apps Script のコードを実装します。リポジトリ構造の定義書に従って `gas/` フォルダに配置します。

> **参照すべきドキュメント**
> - [要件定義書](./要件定義書.md) 第4.1節 … GAS 機能要件（メール送信、送信完了時の更新、API）
> - [API設計書](./API設計書.md) … 参加者取得API（GET）、受付登録API（POST）のリクエスト・レスポンス仕様、CORS、ルーティング
> - [アーキテクチャ設計書](./アーキテクチャ設計書.md) 第3.2節 … GAS の責務、データフロー
> - [リポジトリ構造の定義書](./リポジトリ構造の定義書.md) 第2章・第6章 … gas/ ディレクトリ構造、.claspignore

### 2.1 リポジトリ構造の整備

- [x] `gas/` フォルダを作成する
- [x] `gas/appsscript.json` を作成する（`timeZone`, `runtimeVersion` 等を設定）
- [x] ルートの `appsscript.json` を `gas/` に移動するか、`gas/` 用に新規作成する
- [x] `.clasp.json` の `rootDir` を `gas` に設定する
- [x] `.claspignore` を作成し、`docs/`, `README.md`, `index.html`, `css/`, `js/`, `assets/` を除外する

### 2.2 スプレッドシート初期設定機能

- [x] `gas/Spreadsheet.gs` または `gas/Setup.gs` に初期設定関数を実装する
- [x] 参加者データシートを作成し、1行目にヘッダー行を設定する
  - 列: ID, メールアドレス, 氏名, 学籍番号, 所属課程, 住まい, 下書き作成日時, 送信日時, 送信ステータス, 午前受付ステータス, 午前受付日時, 午前受付担当者, 午後受付ステータス, 午後受付日時, 午後受付担当者, 集合教室, COE申請, チェックボックス
- [x] メール設定シートを作成し、項目（送信者名, 返信先アドレス, 返信先名, CCアドレス, 件名, 一斉送信メール本文）を配置する
- [x] `onOpen()` のカスタムメニューに「スプレッドシート初期設定」を追加する
- [ ] 初期設定実行時に、既存シートがある場合は上書きするか確認する処理を実装する（任意）

### 2.3 定数・設定の定義

- [x] `gas/Config.gs` を作成する（または既存ファイルに追加）
- [x] シート名（参加者データ、メール設定）を定数で定義する
- [x] 参加者データの列インデックス（A=1, B=2...）を定数で定義する

### 2.4 スプレッドシート操作モジュール

- [x] `gas/Spreadsheet.gs` を作成する
- [x] ID で参加者を検索する関数を実装する
- [x] 参加者データの取得（行データをオブジェクトに変換）を実装する
- [x] 受付ステータス・受付日時・受付担当者を更新する関数を実装する（modeパラメータ（am/pm）を受け取り、午前または午後の対応列に書き込む）
- [x] 送信日時・送信ステータスを更新する関数を実装する
- [x] 下書き作成日時を更新する関数を実装する
- [x] メール設定の読み取り関数を実装する

### 2.5 Web API モジュール

- [x] `gas/Api.gs` を作成する
- [x] `doGet(e)` を実装する
  - [x] `e.parameter.action === 'getParticipant'` の分岐を実装する
  - [x] `id` パラメータを取得し、参加者データを検索する
  - [x] 成功時: JSON で `{ success: true, data: {...} }` を返す
  - [x] 失敗時: JSON で `{ success: false, error: {...} }` を返す
  - [x] CORS ヘッダ `Access-Control-Allow-Origin: *` を付与する（GAS Web アプリ「全員」デプロイで対応）
- [x] `doPost(e)` を実装する
  - [x] `e.postData.contents` を JSON パースする
  - [x] `action === 'registerReception'` の分岐を実装する
  - [x] `id`, `staff`, `mode` を取得し、受付登録処理を呼び出す（modeで午前/午後を振り分け）
  - [x] 成功・失敗に応じた JSON レスポンスを返す
  - [x] CORS ヘッダを付与する（GAS Web アプリ「全員」デプロイで対応）

### 2.6 メール送信モジュール

- [x] `gas/Mail.gs` を作成する
- [x] GoQR API を使って QR コード画像 URL を生成する関数を実装する
  - URL: `https://api.qrserver.com/v1/create-qr-code/?data={encodeURIComponent(id)}&size=200x200&format=png`
  - 参考: [技術選定チェック結果](./技術選定チェック結果.md)
- [x] メール本文に参加者情報（氏名、学籍番号、所属課程、ID）を差し込む関数を実装する
- [x] メール本文に QR コード画像を挿入する処理を実装する
- [x] 下書き一括作成関数を実装する
  - [x] 参加者データからメールアドレスを取得
  - [x] 各参加者ごとに HTML メールの下書きを作成
  - [x] 下書き作成日時を参加者データに記録する
- [x] 一斉送信関数を実装する
  - [x] Gmail の下書きを取得して送信
  - [x] 送信完了後に参加者データの「送信日時」「送信ステータス」を更新する

### 2.7 メニュー・ダイアログ

- [x] `gas/Code.gs` に `onOpen()` を実装し、カスタムメニューを追加する
- [x] 「メール設定」メニューからメール設定ダイアログを開く処理を実装する
- [x] 「下書き一括作成」メニューを追加する
- [x] 「一斉送信」メニューを追加する
- [ ] メール設定用の HTML ダイアログ（`gas/MailSettings.html` 等）を作成する（必要に応じて）

### 2.8 GAS の動作確認

- [x] `clasp push` でコードを GAS にプッシュする
- [x] `clasp open` で GAS エディタを開く
- [x] メニューから「スプレッドシート初期設定」を実行し、参加者データシート・メール設定シートが作成されることを確認する
- [x] 参加者データにテスト用データを1〜2件入力する（ID、氏名、学籍番号、所属課程、集合教室、メールアドレスなど）
- [x] 参加者データの読み書きが正しく動作することを確認する

---

## Phase 3: フロントエンド（QR読み取りアプリ）実装

QRコード読み取りアプリを実装します。リポジトリ構造の定義書に従い、ルート直下に `index.html`, `css/`, `js/`, `assets/` を配置します。

> **参照すべきドキュメント**
> - [要件定義書](./要件定義書.md) 第4.3〜4.6節・第9章 … フロントエンド機能、午前/午後モードの表示項目、エラー・例外処理
> - [API設計書](./API設計書.md) 第4〜5章・第8章 … getParticipant / registerReception の呼び出し方、フロントエンドからの利用フロー
> - [アーキテクチャ設計書](./アーキテクチャ設計書.md) 第3.1節・第4.2節 … フロントエンドの責務、受付フロー
> - [リポジトリ構造の定義書](./リポジトリ構造の定義書.md) 第6章 … ルート直下のディレクトリ構造（index.html, css/, js/, assets/）
> - [技術選定チェック結果](./技術選定チェック結果.md) … html5-qrcode の導入方法、GoQR API の仕様

### 3.1 ディレクトリ・ファイルの作成

- [x] ルート直下に `index.html` を作成する
- [x] `css/` フォルダを作成し、`style.css` を配置する
- [x] `js/` フォルダを作成する
- [x] `assets/` フォルダを作成する
- [x] スキャン成功音（`assets/success.mp3`）を用意する（Web Audio API でビープ音を実装）
- [x] エラー音（`assets/error.mp3`）を用意する（Web Audio API でビープ音を実装）

### 3.2 設定ファイル

- [x] `js/config.js` を作成する
- [x] GAS Web アプリ URL を設定する（後でデプロイ後に更新）
- [x] 午前モードのデフォルト表示項目（ID、氏名、所属課程）を定義する
- [x] 午後モードのデフォルト表示項目（氏名、集合教室）を定義する

### 3.3 基本 HTML 構造

- [x] `index.html` に基本的な HTML5 構造を記述する
- [x] カメラ表示用の `<video>` 要素を配置する
- [x] QR コード検出用の `<canvas>` 要素を配置する（ライブラリの要件に応じて）
- [x] 参加者情報表示用のエリアを配置する
- [x] 午前モード・午後モード切り替え用のボタンまたはスイッチを配置する
- [x] 担当者選択用のドロップダウンを配置する
- [x] エラー表示用のエリアを配置する

### 3.4 QR 読み取り機能

- [x] **html5-qrcode** を CDN で導入する（`https://unpkg.com/html5-qrcode`）
  - 参考: [技術選定チェック結果](./技術選定チェック結果.md)
- [x] `js/qr-scanner.js` を作成する
- [x] `Html5Qrcode` クラス（Pro Mode）でカスタムUI を実装する
- [x] `facingMode: "environment"` でスマホ背面カメラを指定する（午前モード）
- [x] QR コード検出時に `decodedText`（参加者ID）をコールバックで渡す処理を実装する
- [x] モード切り替え時は `html5QrCode.stop()` でカメラを停止する

### 3.5 API 連携

- [x] `js/api.js` または `app.js` 内に API 呼び出し関数を実装する
- [x] `getParticipant(id)` … GET で参加者情報を取得する
- [x] `registerReception(id, staff, mode)` … POST で受付登録する（modeは'am'または'pm'）
- [x] エラーレスポンス時の処理を実装する

### 3.6 メインアプリロジック

- [x] `js/app.js` を作成する
- [x] 午前モード・午後モードの状態を管理する
- [x] モード切り替えボタンのイベントハンドラを実装する
- [x] 担当者選択の値を保持する
- [x] QR スキャン時のフローを実装する
  1. ID 取得
  2. `getParticipant(id)` 呼び出し
  3. `success: false` の場合 → エラー表示・エラー音
  4. `success: true` の場合 → `registerReception(id, staff)` 呼び出し
  5. 参加者情報を画面に表示（午前: ID・氏名・所属課程、午後: 氏名・集合教室）
  6. スキャン成功音を再生
  7. 既に受付済みなら「受付済み」と表示する

### 3.7 表示項目のカスタマイズ

- [x] 表示項目を設定で変更できる仕組みを実装する（localStorage や設定画面）
- [x] 午前モード・午後モードそれぞれで表示項目を変更可能にする

### 3.8 音声・エラー処理

- [x] スキャン成功時に `success.mp3` を再生する
- [x] ID 未登録時に `error.mp3` を再生する
- [x] エラー時に画面にエラーメッセージを表示する
- [x] カメラアクセス拒否時にメッセージを表示する
- [x] ネットワークエラー時に適切なメッセージを表示する

### 3.9 スタイリング

- [x] `css/style.css` でレイアウトを整える
- [x] スマートフォン表示に最適化する（午前モード）
- [x] PC 表示に最適化する（午後モード）
- [x] ボタン・スイッチの見た目を整える

### 3.10 ローカルでの動作確認

- [x] ローカルサーバー（例: `npx serve .` や VS Code の Live Server）で起動する
- [x] カメラが起動することを確認する（HTTPS または localhost が必要）
- [x] GAS API が未デプロイの場合は、モックデータで表示確認する

---

## Phase 4: 統合・結合テスト

GAS とフロントエンドを連携させ、全体の動作を確認します。

> **参照すべきドキュメント**
> - [API設計書](./API設計書.md) 第4〜5章 … 成功・エラー時のレスポンス形式、テスト観点
> - [要件定義書](./要件定義書.md) 第7章・第9章 … 運用フロー、エラー・例外処理の期待動作
> - [アーキテクチャ設計書](./アーキテクチャ設計書.md) 第4.2節 … 受付フロー（ID存在時・不存在時）

### 4.1 GAS Web アプリのデプロイ（テスト用）

- [x] GAS エディタで「デプロイ」→「新しいデプロイ」を開く
- [x] 種類で「ウェブアプリ」を選択する
- [x] 説明に「テスト用」等を入力する
- [x] 「次のユーザーとして実行」を「自分」に設定する
- [x] 「アクセスできるユーザー」を「全員」に設定する（テスト時は「自分のみ」でも可）
- [x] デプロイを実行し、**ウェブアプリの URL** をコピーする

### 4.2 フロントエンドの設定更新

- [x] `js/config.js` の GAS Web アプリ URL を、デプロイした URL に更新する

### 4.3 結合テスト

- [ ] フロントエンドを起動し（ローカルまたは GitHub Pages）、QR コードをスキャンする
- [ ] テスト用 QR コードを生成する（参加者データの ID をエンコードした QR コード）
  - オンライン QR コード生成サイトや、スマートフォンの QR コード表示アプリを利用
- [ ] 参加者情報が正しく表示されることを確認する
- [ ] 受付登録後、スプレッドシートの午前/午後それぞれの受付ステータス・受付日時・受付担当者が正しい列に更新されることを確認する
- [ ] スキャン成功音が鳴ることを確認する
- [ ] 存在しない ID の QR をスキャンし、エラー表示・エラー音が出ることを確認する
- [ ] 重複スキャンで「受付済み」と表示されることを確認する
- [ ] 午前モード・午後モードの切り替えが正しく動作することを確認する

### 4.4 メール送信のテスト（任意）

- [x] メール設定ダイアログでテスト用の設定を入力する
- [x] 下書き一括作成を実行し、Gmail に下書きが作成されることを確認する
- [x] 下書き作成日時が参加者データに記録されることを確認する
- [x] 一斉送信を実行し（テスト用アドレスに送信）、送信日時・送信ステータスが更新されることを確認する

---

## Phase 5: 本番デプロイ

本番環境へのデプロイを行い、運用準備を整えます。

> **参照すべきドキュメント**
> - [リポジトリ構造の定義書](./リポジトリ構造の定義書.md) 第7章 … GAS デプロイ手順、GitHub へのプッシュ、GitHub Pages 有効化
> - [アーキテクチャ設計書](./アーキテクチャ設計書.md) 第6章 … デプロイ方式、各コンポーネントのデプロイ方法
> - [要件定義書](./要件定義書.md) 第5章 … GAS Web API アクセス制御（「全員」でデプロイする推奨設定）

### 5.1 GAS の本番デプロイ

- [ ] `clasp push` で最新コードを GAS にプッシュする
- [ ] GAS エディタで「デプロイ」→「デプロイを管理」を開く
- [ ] 既存のテスト用デプロイを「本番」に更新するか、新規デプロイを作成する
- [ ] 「アクセスできるユーザー」を「全員」に設定する（課内スタッフが使えるように）
- [ ] 本番用の **ウェブアプリ URL** をコピーする
- [ ] フロントエンドの `js/config.js` を本番 URL に更新する（未反映の場合）

### 5.2 GitHub リポジトリの準備

- [ ] `git init` でリポジトリを初期化する（未初期化の場合）
- [ ] `.gitignore` を作成し、`node_modules/` 等を除外する
- [ ] `README.md` を作成し、プロジェクト概要・セットアップ手順を記載する

### 5.3 GitHub へのプッシュ

- [ ] `git add .` で変更をステージする
- [ ] `git commit -m "Initial implementation"` でコミットする
- [ ] `gh repo create QRcodeSystemforOrientation --private --source=. --push` でリポジトリを作成してプッシュする
  - または既存リポジトリに `git remote add origin ...` で紐付け、`git push -u origin main` を実行する

### 5.4 GitHub Pages の有効化

- [ ] GitHub リポジトリの「Settings」→「Pages」を開く
- [ ] 「Source」で「Deploy from a branch」を選択する
- [ ] 「Branch」を `main`、「Folder」を `/ (root)` に設定する
- [ ] 保存し、数分待ってから `https://{owner}.github.io/QRcodeSystemforOrientation/` にアクセスする
- [ ] QR 読み取りアプリが表示されることを確認する

### 5.5 最終動作確認

- [ ] GitHub Pages の URL からアプリを開く
- [ ] スマートフォンで午前モードの動作を確認する
- [ ] PC で午後モードの動作を確認する
- [ ] 本番 GAS URL と連携していることを確認する
- [ ] スタッフ向けの利用手順書を用意する（任意）

---

## Phase 6: 運用・保守（参考）

本番稼働後の運用タスクです。チェックリストとして必要に応じて使用してください。

> **参照すべきドキュメント**
> - [リポジトリ構造の定義書](./リポジトリ構造の定義書.md) 第7.3節 … 更新時のデプロイ（GAS / フロントエンド）
> - [要件定義書](./要件定義書.md) 第7章 … 事前準備・当日の運用フロー

### 6.1 定期的な確認

- [ ] 学期ごとに参加者データをクリアまたは新規シートで管理する
- [ ] メール設定を新学期用に更新する
- [ ] 担当者リストを更新する（フロントエンドのドロップダウン用）

### 6.2 障害時の対応

- [ ] GAS の実行ログを確認する（「実行数」から）
- [ ] スプレッドシートの権限を確認する
- [ ] GitHub Pages のビルド状況を確認する

### 6.3 更新時のデプロイ

- [ ] GAS のコード変更時: `clasp push` → GAS エディタで「デプロイ」→「デプロイを管理」→「バージョンを編集」で更新
- [ ] フロントエンドの変更時: `git add .` → `git commit` → `git push` で GitHub にプッシュ（自動で GitHub Pages が更新される）

---

## タスク一覧サマリ

| Phase | タスク数（目安） | 主な成果物 |
|-------|------------------|------------|
| Phase 0 | 約10 | 環境構築完了 |
| Phase 1 | 約15 | スプレッドシート・GAS 基盤 |
| Phase 2 | 約35 | GAS コード一式 |
| Phase 3 | 約35 | フロントエンド一式 |
| Phase 4 | 約15 | 結合テスト完了 |
| Phase 5 | 約15 | 本番デプロイ完了 |

---

## 参考ドキュメント

- [要件定義書](./要件定義書.md)
- [アーキテクチャ設計書](./アーキテクチャ設計書.md)
- [API設計書](./API設計書.md)
- [リポジトリ構造の定義書](./リポジトリ構造の定義書.md)
- [技術選定チェック結果](./技術選定チェック結果.md)

---

*最終更新: 2025年3月*
